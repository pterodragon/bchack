module.exports = {
  testEnvironment: "node",
  testMatch: ["**/*.test.ts"],
  roots: [ "<rootDir>" ],
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'node'],
  preset: "ts-jest",
  verbose: true,
}
