import { BigNumber, ethers } from "ethers"
const { AddressZero, HashZero } = ethers.constants;

const AppDefinition: string = AddressZero;
const AppData: string = HashZero;

export {AppData, AppDefinition}